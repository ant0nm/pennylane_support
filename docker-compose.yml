services:
  pennylane-support-pg:
    image: postgres:17
    env_file:
      - path: ./.env
    ports:
      - "5432:5432"
    volumes:
      - ~/docker-data/pennylane-support-pg:/var/lib/postgresql/data
